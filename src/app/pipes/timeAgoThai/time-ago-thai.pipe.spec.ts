import { TimeAgoThaiPipe } from './time-ago-thai.pipe';

describe('TimeAgoThaiPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoThaiPipe();
    expect(pipe).toBeTruthy();
  });
});
